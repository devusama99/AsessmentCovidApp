import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#74117C",
      light: "#F9F2FF",
    },
    secondary: {
      main: "#C9266B",
    },
    light: {
      main: "#f1f2f6",
    },
    dark: {
      main: "#2f3542",
    },
  },
});

export default theme;
